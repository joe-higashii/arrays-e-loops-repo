// criando um array de 5 numeros

const nomeDeVilas = ["Amegakure", "Konohagakure", "Sunagakure", "Otogakure", "Kirigakure"]

let indice = 0;

// enquanto o indice for menor que o tamanho do array
while (indice < nomeDeVilas.length) {   // executar essa linha
    console.log(nomeDeVilas[indice]);
    indice++;
}
